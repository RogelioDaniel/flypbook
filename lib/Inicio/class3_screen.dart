import 'package:flutter/material.dart';

class Class3Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Class 3'),
      ),
      body: Center(
        child: Text('This is Class 3 Screen'),
      ),
    );
  }
}
