import 'package:flutter/material.dart';

class Class2Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Class 2'),
      ),
      body: Center(
        child: Text('This is Class 2 Screen'),
      ),
    );
  }
}
